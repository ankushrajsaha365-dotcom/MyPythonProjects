def listy():
    l = []
    n = int(input("Enter number of elements: "))

    for _ in range(n):
        element = int(input("Enter element: "))
        l.append(element)

    return l


#### using this module in other list related problems
# To know more about modules, look at the module directory 😁